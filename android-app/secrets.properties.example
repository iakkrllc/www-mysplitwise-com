# Copy this file to secrets.properties (gitignored) and fill in real values once
# the Supabase project credentials are provided. Until then, app/build.gradle.kts
# falls back to harmless placeholders so the project builds out of the box.
SUPABASE_URL=https://replace-me.supabase.co
SUPABASE_ANON_KEY=replace-me-with-real-anon-key
